import React from 'react';
import { Location, History } from 'history';
import { match, withRouter } from 'react-router-dom';
import { compose } from 'recompose';
import {
{{camelCase name}}Reducer,
{{camelCase name}}StateInit,
{{pascalCase name}}StateActionTypes,
} from './{{pascalCase name}}Reducer';
import s from './{{pascalCase name}}.module.scss';

interface Props {
  location?: Location;
  match?: match;
  history?: History;
}

const {{pascalCase name}}View: React.FC<Props> = (props: Props) => {
  const [{{camelCase name}}State, dispatch] = React.useReducer(
    {{camelCase name}}Reducer,
    {},
    {{camelCase name}}StateInit,
  );

  return (
    <div className={s.{{pascalCase name}} } >
      {{pascalCase name}}...
    </div>
  );
};

export default compose<Props, Props>(withRouter)({{pascalCase name}}View);