interface {{pascalCase name}}State { }

enum {{pascalCase name}}StateActionTypes { }

interface {{pascalCase name}}StateAction {
  type: {{pascalCase name}}StateActionTypes;
}

const {{camelCase name}}StateInit = (
  initialState: {{pascalCase name}}State,
): {{pascalCase name}}State => {
  return {
    ...initialState
  };
};

const {{camelCase name}}Reducer = (
  state: {{pascalCase name}}State,
  action: {{pascalCase name}}StateAction,
): {{pascalCase name}}State => {
  switch (action.type) {
    default:
      return state;
  }
};

export {
  {{pascalCase name}}StateActionTypes,
  {{camelCase name}}StateInit,
  {{camelCase name}}Reducer,
};